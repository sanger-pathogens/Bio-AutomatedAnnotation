#!/usr/bin/env perl

package Bio::AutomatedAnnotation::Main::EukaryotesAutomatedAnnotation;

# ABSTRACT: Annotate some eukaryotes
# PODNAME: annotate_eukaryotes

=head1 SYNOPSIS

Wrapper script to annotate eukaryotes.

=cut

BEGIN { unshift( @INC, '../lib' ) }
BEGIN { unshift( @INC, './lib' ) }
BEGIN { unshift( @INC, '/software/pathogen/internal/prod/lib/' ) }
use Bio::AutomatedAnnotation::CommandLine::AnnotateEukaryotes;

Bio::AutomatedAnnotation::CommandLine::AnnotateEukaryotes->new(args => \@ARGV, script_name => $0)->run;
